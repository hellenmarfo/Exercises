#import turtle
#import math

#bob = turtle.Turtle()

#def arc(t, r, angle):
    #n = int(100 * angle / 360)
    #arc_length = 2 * math.pi * r * angle / 360
    #step_length = arc_length / n
    #step_angle = angle / n

    #for i in range(n):
        #t.forward(step_length)
        #t.left(step_angle)

#arc(bob, 100, 180)   # Half circle

#turtle.done()